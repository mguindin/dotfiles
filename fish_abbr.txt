abbr -a -U -- arn 'asdf reshim nodejs'
abbr -a -U -- b bat
abbr -a -U -- be 'bundle exec'
abbr -a -U -- dcb 'docker-compose build'
abbr -a -U -- dcd 'docker-compose down -v'
abbr -a -U -- dcs 'docker-compose stop'
abbr -a -U -- dcu 'docker-compose up --build -d'
abbr -a -U -- dps 'docker ps'
abbr -a -U -- ds 'docker-compose stop'
abbr -a -U -- gcm 'git checkout master'
abbr -a -U -- gco 'git checkout'
abbr -a -U -- gd 'git diff'
abbr -a -U -- gp 'git pull'
abbr -a -U -- gpm 'git pull origin master'
abbr -a -U -- gpull 'git pull'
abbr -a -U -- mux tmuxinator
abbr -a -U -- nv nvim
abbr -a -U -- pa 'pyenv activate'
abbr -a -U -- pd 'pyenv deactivate'
abbr -a -U -- s 'git status'
abbr -a -U -- sf 'source $HOME/.config/fish/config.fish'
abbr -a -U -- t tldr
abbr -a -U -- ta tn
abbr -a -U -- tl 'tmux ls'
abbr -a -U -- vfish 'vim $HOME/.config/fish/config.fish'
abbr -a -U -- vg 'vim $HOME/.vim/general.vim'
abbr -a -U -- vp 'vim $HOME/.vim/plugins.vim'
